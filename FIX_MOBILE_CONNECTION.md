# 🔧 إصلاح مشكلة الاتصال على الجوال

## المشكلة
التطبيق لا يعمل على الجوال لأن التطبيق لا يعرف عنوان السيرفر للاتصال به.

## الحل المطبق

تم تحديث الكود ليكتشف عنوان السيرفر تلقائياً بالترتيب التالي:

1. **NGROK_URL** (من ملف `.env`)
2. **VITE_API_URL** (من ملف `.env`)
3. **REPLIT_URL** (من ملف `.env`)
4. **Local IP** (تلقائياً في وضع التطوير)

## خطوات التشغيل

### الطريقة 1: استخدام ngrok (موصى به)

1. **شغّل السيرفر:**
   ```bash
   npm run dev
   ```

2. **شغّل ngrok (في terminal جديد):**
   ```bash
   ngrok http 5000
   ```

3. **انسخ الـ URL من ngrok** (مثل: `https://abc123.ngrok.io`)

4. **حدّث ملف `.env`:**
   ```env
   NGROK_URL=https://abc123.ngrok.io
   VITE_API_URL=https://abc123.ngrok.io
   ```

5. **ابني التطبيق:**
   ```bash
   npm run build
   ```

6. **مزامنة Capacitor:**
   ```bash
   npm run cap:sync
   ```

7. **افتح Xcode:**
   ```bash
   npm run cap:open:ios
   ```

8. **شغّل التطبيق من Xcode**

### الطريقة 2: استخدام الشبكة المحلية

1. **شغّل السيرفر:**
   ```bash
   npm run mobile:dev
   ```
   
   هذا سيعرض IP المحلي (مثل: `http://192.168.1.100:5000`)

2. **تأكد من أن الجوال على نفس شبكة WiFi**

3. **ابني التطبيق:**
   ```bash
   npm run build
   ```
   
   الكود سيكتشف IP المحلي تلقائياً ويستخدمه

4. **مزامنة Capacitor:**
   ```bash
   npm run cap:sync
   ```

5. **افتح Xcode:**
   ```bash
   npm run cap:open:ios
   ```

6. **شغّل التطبيق**

## التغييرات المطبقة

### 1. `vite.config.ts`
- تم إضافة منطق لاكتشاف عنوان السيرفر تلقائياً
- يتم تمرير `VITE_SERVER_URL` إلى التطبيق عند البناء

### 2. `client/src/lib/config.ts`
- تم تحديث `getApiBaseUrl()` لاستخدام `VITE_SERVER_URL` أولاً
- إذا لم يكن موجوداً، يحاول استخدام `window.location.origin` في Capacitor
- يعطي رسائل تحذير واضحة إذا لم يتم العثور على URL

## التحقق من أن كل شيء يعمل

1. **افتح Console في Xcode** عند تشغيل التطبيق
2. **ابحث عن رسالة:** `[Config] Using build-time server URL: ...`
3. **إذا رأيت هذه الرسالة**، يعني أن التطبيق وجد عنوان السيرفر بنجاح

## حل المشاكل

### المشكلة: التطبيق لا يتصل بالسيرفر

**الحل:**
1. تأكد من أن السيرفر يعمل (`npm run dev`)
2. تأكد من أن ngrok يعمل (إذا كنت تستخدمه)
3. تحقق من ملف `.env` - يجب أن يحتوي على `NGROK_URL` أو `VITE_API_URL`
4. أعد البناء: `npm run build && npm run cap:sync`

### المشكلة: رسالة "Running in Capacitor but no API URL configured"

**الحل:**
1. أضف `NGROK_URL` أو `VITE_API_URL` في ملف `.env`
2. أعد البناء: `npm run build && npm run cap:sync`
3. أعد تشغيل التطبيق

### المشكلة: التطبيق يعرض صفحة بيضاء

**الحل:**
1. تحقق من Console في Xcode للأخطاء
2. تأكد من أن البناء نجح: `npm run build`
3. تأكد من المزامنة: `npm run cap:sync`
4. أعد فتح Xcode: `npm run cap:open:ios`

## ملاحظات مهمة

- **بعد كل تغيير في `.env`:** يجب إعادة البناء (`npm run build`) والمزامنة (`npm run cap:sync`)
- **للتطوير السريع:** استخدم `npm run mobile:dev` لعرض IP المحلي
- **للإنتاج:** تأكد من إزالة `server.url` من `capacitor.config.ts` أو عدم وجود `NGROK_URL` في `.env`

## ✅ قائمة التحقق

- [ ] السيرفر يعمل (`npm run dev`)
- [ ] ngrok يعمل (إذا كنت تستخدمه)
- [ ] ملف `.env` محدّث بـ `NGROK_URL` أو `VITE_API_URL`
- [ ] التطبيق مبني (`npm run build`)
- [ ] Capacitor مزامن (`npm run cap:sync`)
- [ ] Xcode مفتوح (`npm run cap:open:ios`)
- [ ] التطبيق يعمل على الجوال

---

**تم! 🎉** التطبيق يجب أن يعمل الآن على الجوال.












