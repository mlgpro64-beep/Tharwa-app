# 🔧 استكشاف الأخطاء - التطبيق لا يعمل

## ✅ ما تم التحقق منه:

- ✅ السيرفر يعمل على port 5000
- ✅ الواجهة الأمامية متاحة
- ✅ Paylink credentials موجودة
- ✅ DATABASE_URL موجود

---

## 🔍 المشاكل المحتملة وحلولها:

### 1️⃣ تطبيق iOS لا يتصل بالسيرفر

**المشكلة:** تطبيق iOS يحتاج URL صحيح

**الحل:**
- تأكد من أن `VITE_API_URL` في `.env` صحيح
- للتطوير المحلي: استخدم ngrok URL
- للتطبيق iOS: استخدم ngrok URL في `capacitor.config.ts`

### 2️⃣ السيرفر لا يستجيب

**المشكلة:** السيرفر متوقف أو به خطأ

**الحل:**
```bash
# أوقف السيرفر (Ctrl+C)
# ثم شغّله مرة أخرى:
npm run dev
```

### 3️⃣ خطأ في قاعدة البيانات

**المشكلة:** DATABASE_URL غير صحيح أو Migration لم يتم

**الحل:**
```bash
# تحقق من DATABASE_URL في .env
# شغّل Migration:
npm run db:push
```

### 4️⃣ Paylink لا يعمل

**المشكلة:** Paylink credentials غير صحيحة

**الحل:**
- تحقق من `PAYLINK_APP_ID` و `PAYLINK_SECRET_KEY` في `.env`
- تأكد من أن Paylink API keys صحيحة

### 5️⃣ ngrok لا يعمل

**المشكلة:** ngrok URL تغير أو ngrok متوقف

**الحل:**
```bash
# شغّل ngrok:
ngrok http 5000

# انسخ الـ URL الجديد
# حدّث VITE_API_URL في .env
# حدّث Webhook URL في Paylink
```

---

## 🧪 خطوات التشخيص:

### 1. تحقق من السيرفر:
```bash
# افتح المتصفح:
http://localhost:5000

# يجب أن ترى الواجهة الأمامية
```

### 2. تحقق من API:
```bash
# افتح:
http://localhost:5000/api/payments

# يجب أن ترى response (حتى لو كان 401)
```

### 3. تحقق من ngrok:
```bash
# افتح:
https://8593d2bb6452.ngrok-free.app

# يجب أن ترى الواجهة الأمامية
```

### 4. تحقق من Terminal:
- ابحث عن أخطاء في terminal السيرفر
- ابحث عن: `[Paylink] Initialized successfully`
- ابحث عن: `Server listening on port 5000`

---

## 📱 لتطبيق iOS:

### إذا كان التطبيق لا يتصل:

1. **تحقق من capacitor.config.ts:**
```typescript
server: {
  url: 'https://8593d2bb6452.ngrok-free.app',
}
```

2. **أعد بناء التطبيق:**
```bash
npm run build
npx cap sync ios
```

3. **شغّل ngrok:**
```bash
ngrok http 5000
```

---

## 🆘 إذا لم يعمل شيء:

1. **أوقف كل شيء:**
   - أوقف السيرفر (Ctrl+C)
   - أوقف ngrok (Ctrl+C)

2. **أعد التشغيل:**
```bash
# Terminal 1: السيرفر
npm run dev

# Terminal 2: ngrok
ngrok http 5000
```

3. **تحقق من الأخطاء:**
   - راجع terminal السيرفر
   - راجع browser console
   - راجع ngrok web interface: http://localhost:4040

---

## 📞 معلومات للمساعدة:

- **السيرفر:** http://localhost:5000
- **ngrok:** https://8593d2bb6452.ngrok-free.app
- **Paylink Dashboard:** https://my.paylink.sa/

---

## ✅ اختبار سريع:

افتح في المتصفح:
```
http://localhost:5000
```

يجب أن ترى الواجهة الأمامية للتطبيق.

إذا لم تراها، راجع terminal السيرفر للأخطاء.













