# 🏠 إعداد العمل المحلي على الجوال

## الخطوات السريعة (بدون ngrok)

### 1️⃣ شغّل السيرفر مع عرض IP المحلي

```bash
npm run mobile:dev
```

هذا الأمر سيعرض IP المحلي الخاص بك (مثل: `http://192.168.1.100:5000`)

**مثال على المخرجات:**
```
📱 Local IP Addresses for Mobile Access:

   http://192.168.1.100:5000

💡 Make sure your mobile device is on the same WiFi network.
```

### 2️⃣ تأكد من أن الجوال على نفس شبكة WiFi

- تأكد من أن الكمبيوتر والجوال متصلان بنفس شبكة WiFi
- سجّل الـ IP الذي ظهر في Terminal (مثل: `192.168.1.100`)

### 3️⃣ ابني التطبيق

```bash
npm run build
```

**ملاحظة:** الكود سيكتشف IP المحلي تلقائياً ويستخدمه عند البناء.

### 4️⃣ مزامنة Capacitor

```bash
npm run cap:sync
```

أو استخدم الأمر المدمج:

```bash
npm run mobile:build
```

(هذا سيبني ويمزامن تلقائياً)

### 5️⃣ افتح Xcode

```bash
npm run cap:open:ios
```

### 6️⃣ شغّل التطبيق

- اختر جهاز iOS أو Simulator
- اضغط زر التشغيل (▶️)

---

## ✅ التحقق من أن كل شيء يعمل

### في Xcode Console:

عند تشغيل التطبيق، يجب أن ترى رسالة مثل:

```
[Config] Using build-time server URL: http://192.168.1.100:5000
```

إذا رأيت هذه الرسالة، يعني أن التطبيق وجد عنوان السيرفر بنجاح! ✅

---

## 🔧 حل المشاكل

### المشكلة: لا يظهر IP المحلي

**الحل:**
1. تأكد من أنك متصل بشبكة WiFi
2. أعد تشغيل الأمر: `npm run mobile:dev`
3. تحقق من إعدادات الشبكة في Windows

### المشكلة: التطبيق لا يتصل بالسيرفر

**الحل:**
1. تأكد من أن السيرفر يعمل (`npm run mobile:dev`)
2. تأكد من أن الجوال على نفس شبكة WiFi
3. تحقق من Firewall - قد تحتاج إلى السماح للمنفذ 5000
4. أعد البناء: `npm run build && npm run cap:sync`

### المشكلة: رسالة "Running in Capacitor but no API URL configured"

**الحل:**
1. تأكد من أن السيرفر يعمل (`npm run mobile:dev`)
2. تأكد من أن `NODE_ENV=development` (يتم تعيينه تلقائياً في `npm run mobile:dev`)
3. أعد البناء: `npm run build && npm run cap:sync`

### المشكلة: Firewall يمنع الاتصال

**الحل:**

**Windows:**
1. افتح "Windows Defender Firewall"
2. اضغط "Allow an app through firewall"
3. أضف Node.js أو المنفذ 5000

**أو من PowerShell (كمسؤول):**
```powershell
New-NetFirewallRule -DisplayName "Node.js Server" -Direction Inbound -LocalPort 5000 -Protocol TCP -Action Allow
```

---

## 📝 ملاحظات مهمة

1. **بعد كل تغيير في الكود:**
   - شغّل `npm run build`
   - شغّل `npm run cap:sync`
   - أعد تشغيل التطبيق من Xcode

2. **إذا غيرت IP المحلي:**
   - أعد تشغيل `npm run mobile:dev`
   - أعد البناء: `npm run build && npm run cap:sync`

3. **للإنتاج:**
   - لا تستخدم هذه الطريقة
   - استخدم ngrok أو خادم إنتاج حقيقي

---

## 🎯 الأوامر السريعة

```bash
# شغّل السيرفر مع عرض IP
npm run mobile:dev

# بناء ومزامنة
npm run mobile:build

# فتح Xcode
npm run cap:open:ios
```

---

## ✅ قائمة التحقق

- [ ] السيرفر يعمل (`npm run mobile:dev`)
- [ ] الجوال على نفس شبكة WiFi
- [ ] IP المحلي ظاهر في Terminal
- [ ] التطبيق مبني (`npm run build`)
- [ ] Capacitor مزامن (`npm run cap:sync`)
- [ ] Xcode مفتوح (`npm run cap:open:ios`)
- [ ] التطبيق يعمل على الجوال
- [ ] رسالة `[Config] Using build-time server URL` تظهر في Console

---

**تم! 🎉** التطبيق يعمل الآن محلياً على الجوال!












