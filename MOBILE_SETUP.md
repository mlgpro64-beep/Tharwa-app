# 📱 دليل تشغيل التطبيق على الجوال

## ✅ المتطلبات الأساسية

1. **Node.js** و **npm** مثبتين
2. **Xcode** (لـ iOS) أو **Android Studio** (لـ Android)
3. **Capacitor CLI** (مثبت تلقائياً مع المشروع)

---

## 🚀 خطوات التشغيل على الجوال

### الطريقة 1: استخدام ngrok (موصى به للتطوير)

#### 1️⃣ شغّل السيرفر

```bash
npm run dev
```

السيرفر سيعمل على `http://localhost:5000`

#### 2️⃣ شغّل ngrok (في terminal جديد)

```bash
ngrok http 5000
```

ستحصل على URL مثل: `https://abc123.ngrok.io`

#### 3️⃣ حدّث ملف `.env`

أضف أو حدّث السطر التالي:

```env
NGROK_URL=https://abc123.ngrok.io
VITE_API_URL=https://abc123.ngrok.io
```

(استبدل `abc123.ngrok.io` بالـ URL الفعلي من ngrok)

#### 4️⃣ ابني التطبيق

```bash
npm run build
```

#### 5️⃣ مزامنة Capacitor

```bash
npm run cap:sync
```

أو:

```bash
npm run mobile:build
```

(هذا سيبني ويمزامن تلقائياً)

#### 6️⃣ افتح Xcode (لـ iOS)

```bash
npm run cap:open:ios
```

أو:

```bash
npx cap open ios
```

#### 7️⃣ شغّل التطبيق من Xcode

- اختر جهاز iOS أو Simulator
- اضغط زر التشغيل (▶️)

---

### الطريقة 2: استخدام الشبكة المحلية (Local Network)

#### 1️⃣ شغّل السيرفر مع عرض IP المحلي

```bash
npm run mobile:dev
```

هذا سيعرض IP المحلي الخاص بك (مثل: `http://192.168.1.100:5000`)

#### 2️⃣ تأكد من أن الجوال على نفس الشبكة

- تأكد من أن الكمبيوتر والجوال على نفس شبكة WiFi
- سجّل الـ IP الذي ظهر في Terminal

#### 3️⃣ ابني التطبيق

```bash
npm run build
```

#### 4️⃣ مزامنة Capacitor

```bash
npm run cap:sync
```

#### 5️⃣ افتح Xcode

```bash
npm run cap:open:ios
```

#### 6️⃣ شغّل التطبيق

- اختر جهاز iOS أو Simulator
- اضغط زر التشغيل (▶️)

---

## 🔧 الأوامر المتاحة

### أوامر البناء والمزامنة

```bash
# بناء التطبيق
npm run build

# مزامنة Capacitor (يجب تشغيله بعد كل build)
npm run cap:sync

# نسخ الملفات إلى المشروع الأصلي
npm run cap:copy

# بناء ومزامنة معاً
npm run mobile:build
```

### أوامر فتح المشروع

```bash
# فتح مشروع iOS في Xcode
npm run cap:open:ios

# أو مباشرة
npx cap open ios
```

---

## ⚙️ إعدادات capacitor.config.ts

الملف `capacitor.config.ts` يكتشف تلقائياً URL الخادم بالترتيب التالي:

1. **NGROK_URL** (من `.env`)
2. **VITE_API_URL** (من `.env`)
3. **REPLIT_URL** (من `.env`)
4. **Local IP** (تلقائياً في وضع التطوير)

### للتطوير المحلي:

```env
# في ملف .env
NGROK_URL=https://your-ngrok-url.ngrok.io
VITE_API_URL=https://your-ngrok-url.ngrok.io
```

### للإنتاج:

احذف `url` من `server` في `capacitor.config.ts` أو تأكد من عدم وجود `NGROK_URL` في `.env`

---

## 🐛 حل المشاكل الشائعة

### المشكلة 1: التطبيق لا يتصل بالسيرفر

**الحل:**
1. تأكد من أن السيرفر يعمل (`npm run dev`)
2. تأكد من أن ngrok يعمل (إذا كنت تستخدمه)
3. تحقق من `NGROK_URL` أو `VITE_API_URL` في `.env`
4. أعد بناء ومزامنة: `npm run mobile:build`

### المشكلة 2: خطأ "Cannot find module"

**الحل:**
```bash
# أعد تثبيت الحزم
npm install

# أعد بناء التطبيق
npm run build

# مزامنة Capacitor
npm run cap:sync
```

### المشكلة 3: التطبيق يعرض صفحة بيضاء

**الحل:**
1. تحقق من console في Xcode
2. تأكد من أن `webDir` في `capacitor.config.ts` صحيح (`dist/public`)
3. تأكد من أن البناء نجح: `npm run build`
4. أعد المزامنة: `npm run cap:sync`

### المشكلة 4: CORS errors

**الحل:**
- تأكد من أن السيرفر يسمح بـ origin الخاص بك
- تحقق من `allowedOrigins` في `server/index.ts`
- تأكد من أن `NGROK_URL` أو `VITE_API_URL` صحيح

### المشكلة 5: التطبيق لا يفتح في Xcode

**الحل:**
```bash
# تأكد من أن iOS platform مثبت
npx cap add ios

# أعد المزامنة
npm run cap:sync

# افتح Xcode
npm run cap:open:ios
```

---

## 📝 ملاحظات مهمة

1. **بعد كل تغيير في الكود:**
   - شغّل `npm run build`
   - شغّل `npm run cap:sync`
   - أعد تشغيل التطبيق من Xcode

2. **للتطوير السريع:**
   - استخدم `npm run mobile:dev` لعرض IP المحلي
   - استخدم ngrok للوصول من أي مكان

3. **للإنتاج:**
   - احذف `url` من `server` في `capacitor.config.ts`
   - تأكد من أن التطبيق يستخدم الملفات المبنية محلياً

4. **الأمان:**
   - لا تستخدم ngrok في الإنتاج
   - استخدم HTTPS في الإنتاج
   - تأكد من إعدادات CORS بشكل صحيح

---

## ✅ اختبار سريع

1. شغّل السيرفر: `npm run dev`
2. شغّل ngrok: `ngrok http 5000`
3. حدّث `.env` بـ `NGROK_URL`
4. ابني: `npm run mobile:build`
5. افتح Xcode: `npm run cap:open:ios`
6. شغّل التطبيق

إذا نجح كل شيء، يجب أن ترى التطبيق يعمل على الجوال! 🎉

---

## 🆘 إذا لم يعمل شيء

1. **تحقق من Terminal:**
   - ابحث عن أخطاء في terminal السيرفر
   - ابحث عن أخطاء في ngrok

2. **تحقق من Xcode Console:**
   - افتح Xcode
   - اضغط على Console (أسفل الشاشة)
   - ابحث عن أخطاء

3. **تحقق من Browser Console:**
   - افتح التطبيق في المتصفح أولاً
   - تحقق من console للأخطاء

4. **أعد التشغيل من البداية:**
   ```bash
   # أوقف كل شيء
   # ثم:
   npm run build
   npm run cap:sync
   npm run cap:open:ios
   ```

---

## 📞 معلومات إضافية

- **Capacitor Docs**: https://capacitorjs.com/docs
- **Xcode**: https://developer.apple.com/xcode/
- **ngrok**: https://ngrok.com/

---

## ✅ قائمة التحقق

- [ ] السيرفر يعمل (`npm run dev`)
- [ ] ngrok يعمل (إذا كنت تستخدمه)
- [ ] `.env` محدّث بـ `NGROK_URL` أو `VITE_API_URL`
- [ ] التطبيق مبني (`npm run build`)
- [ ] Capacitor مزامن (`npm run cap:sync`)
- [ ] Xcode مفتوح (`npm run cap:open:ios`)
- [ ] التطبيق يعمل على الجوال

---

**تم! 🎉** التطبيق جاهز للعمل على الجوال.












